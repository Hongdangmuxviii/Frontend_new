.container{
  width:min(var(--max), 100% - 48px);
  margin-inline:auto;
}

.nowrap{white-space:nowrap}

/* Header */
.header{
  position:sticky;
  top:0;
  z-index:120;
  background:var(--surface-base);
  border-bottom:1px solid var(--border);
}
.header__top{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:24px;
  padding-top:6px;
  padding-bottom:0;
  min-height:78px;
}
.brand{
  display:flex;
  align-items:center;
  justify-content:flex-start;
  flex:0 0 188px;
  text-decoration:none;
}
.brand__logoImg{
  height:85px;
  display:block;
  margin-top:0;
  object-fit:contain;
}
.brand__logoImg--handwriting{
  height:85px;
}
.brand__mark{
  width:40px;height:40px;
  display:grid;place-items:center;
  border-radius:8px;
  background:rgba(6,25,223,.1);
  color:var(--primary);
  font-weight:700;
  letter-spacing:-.5px;
}
.brand__name{
  color:var(--primary);
  font-weight:700;
  font-size:20px;
  letter-spacing:-.5px;
}

.search{
  position:relative;
  flex:1 1 640px;
  max-width:672px;
  margin-inline:auto;
}
.search__input{
  width:100%;
  padding:9px 41px 9px 17px;
  border:1px solid var(--border-2);
  border-radius:var(--r-full);
  box-shadow:var(--shadow-1);
  outline:none;
  font-size:14px;
  color:var(--text);
  background:var(--surface-base);
}
.search__input::placeholder{color:var(--muted)}
.search__icon{
  position:absolute;
  right:12px; top:50%;
  translate:0 -50%;
  width:18px;height:18px;
  border:0;
  background:transparent;
  cursor:pointer;
}
.search__icon::before{
  content:"";
  display:block;
  width:18px;height:18px;
  border:2px solid var(--muted-2);
  border-radius:50%;
  position:relative;
}
.search__icon::after{
  content:"";
  position:absolute;
  right:1px; bottom:1px;
  width:8px;height:2px;
  background:var(--muted-2);
  rotate:45deg;
  transform-origin:right bottom;
}

.header__nav{
  display:flex;
  align-items:center;
  justify-content:space-between;
  padding-top:13px;
  padding-bottom:16px;
}
.nav{display:flex; align-items:center; gap:32px; flex-wrap:wrap}
.nav__link{
  font-size:14px;
  color:var(--ink);
  text-decoration:none;
  font-weight:500;
}
.nav__link--muted{color:var(--muted)}
.nav__link--active{
  color:var(--primary-strong);
  position:relative;
}
.nav__link--active::after{
  content:'';
  position:absolute;
  left:0;
  right:0;
  bottom:-16px;
  height:4px;
  border-radius:9999px;
  background:var(--primary-strong);
}
.nav__link:hover{opacity:.8}

/* Buttons / chips */
.btn{
  border-radius:var(--r-6);
  border:1px solid transparent;
  padding:9px 17px;
  font-size:14px;
  font-weight:500;
  cursor:pointer;
  box-shadow:var(--shadow-1);
  background:var(--surface-base);
  color:var(--text-body-2);
  display:inline-flex;
  align-items:center;
  justify-content:center;
  gap:8px;
  white-space:nowrap;
  text-decoration:none;
  appearance:none;
  -webkit-appearance:none;
  overflow:hidden;
}
.btn:hover,
.btn:focus,
.btn:visited{
  text-decoration:none;
}
.btn--outline{border-color:var(--border-strong)}
.btn__icon{
  width:24px;height:16px;
  background:linear-gradient(90deg,#4285F4 0 25%,#EA4335 25% 50%,#FBBC05 50% 75%,#34A853 75% 100%);
  border-radius:4px;
}
.btn--primary{
  background:var(--primary);
  color:var(--surface-base);
  border-color:var(--primary);
  border-radius:var(--r-full);
  padding:9px 24px;
}
.btn--ghost{
  background:var(--surface-base);
  color:var(--muted);
  border-color:var(--border);
  border-radius:var(--r-full);
  padding:9px 25px;
  box-shadow:none;
}
.chip{
  background:var(--bg-soft);
  color:var(--muted);
  border-radius:var(--r-full);
  padding:4px 12px;
  font-size:14px;
  text-decoration:none;
  display:inline-flex;
  align-items:center;
}

/* Shared */
.main{padding-top:24px}
.section{padding:48px 0}
.section--popularFonts,
.section:has(.popularFontCard__visual){
  position:relative;
  z-index:1;
}
.section__head{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:24px;
  margin-bottom:24px;
}
.section__title{display:flex; align-items:baseline; gap:8px}
.section__title h2{
  margin:0;
  font-size:24px;
  line-height:32px;
  font-weight:700;
}
.section__title span{
  font-size:16px;
  line-height:24px;
  color:var(--muted);
}

/* Hero (硫붿씤 諛곕꼫 怨듯넻 ?덉씠?꾩썐) */
.hero{
  position:relative;
  background:var(--bg-soft);
  border-radius:var(--r-16);
  padding:40px 48px 36px;
  overflow:hidden;
}
.hero--bannerAlt{
  background:var(--bg-soft);
}
.hero--bannerAlt .hero__blob--a{
  background:#f472b6;
  opacity:.16;
}
.hero--bannerAlt .hero__blob--b{
  background:#f472b6;
  opacity:0;
}
.hero--bannerAlt .hero__blob--c{
  background:#f9a8d4;
  opacity:.14;
}
.hero--bannerAlt .heroPromo__card{
  background:linear-gradient(145deg, rgba(255,255,255,.96), rgba(255,245,252,.9));
}
.hero--bannerAlt .heroPromo__floating{
  background:rgba(255,255,255,.88);
}
.hero--bannerScan{
  background:var(--bg-soft);
}
.hero--bannerScan .hero__blob--a{
  background:#60a5fa;
  opacity:.1;
}
.hero--bannerScan .hero__blob--b{
  background:var(--info-soft-2);
  opacity:.06;
}
.hero--bannerScan .hero__blob--c{
  background:#60a5fa;
  opacity:.08;
}
.hero__bg{position:absolute; inset:0; pointer-events:none}
.hero__blob{
  position:absolute;
  border-radius:50%;
  filter:blur(32px);
  opacity:.10;
}
.hero__blob--a{right:-80px; top:-80px; width:320px; height:320px; background:var(--primary)}
.hero__blob--b{right:-80px; top:-80px; width:320px; height:320px; background:var(--primary)}
.hero__blob--c{left:-80px; bottom:-80px; width:240px; height:240px; background:#60a5fa}

.hero__grid{
  position:relative;
  display:flex;
  /* 醫뚯슦 釉붾줉 媛꾧꺽(?띿뒪?몄? ?곗륫 移대뱶 ?ъ씠 ?щ갚) */
  gap:24px;
  /* 媛?대뱶 ?ㅻⅨ履?湲곕낯 ?щ갚 */
  padding-right:10px;
  box-sizing:border-box;
  align-items:center;
}
.hero__grid--existing{
  display:block;
  /* 1踰?諛곕꼫 理쒖냼 ?믪씠 */
  min-height:420px;
  /* absolute ?곗륫 移대뱶 怨듦컙 ?뺣낫 ??*/
  padding-right:530px;
}
.hero__copy{
  flex:1 1 auto;
  min-width:0;
  display:flex;
  flex-direction:column;
  align-items:flex-start;
  gap:24px;
}
.hero__tag{
  display:inline-flex;
  align-items:center;
  padding:7px 14px;
  border-radius:9999px;
  border:1px solid var(--info-soft);
  background:rgba(255,255,255,.72);
  color:var(--primary);
  font-size:11px;
  letter-spacing:.14em;
  font-weight:700;
}
.hero__copy h1{
  margin:0 0 16px;
  font-size:64px;
  line-height:68px;
  letter-spacing:-.04em;
  color:var(--text);
  font-weight:800;
}
.hero__titleTwoLine{
  margin:0;
  display:flex;
  flex-direction:column;
  gap:4px;
  font-size:46px;
  line-height:54px;
  letter-spacing:-.04em;
  color:var(--text);
  font-weight:800;
}
.hero__titleTwoLine > span{
  display:block;
  white-space:nowrap;
}
.hero__titleTwoLine > span:first-child{
  /* 1踰?諛곕꼫 泥?以??쒕ぉ ?ш린 */
  font-size:68px;
  /* 1踰?諛곕꼫 泥?以?以꾨넂??*/
  line-height:72px;
}
.hero__titleTwoLine > span:last-child{
  /* 1踰?諛곕꼫 ?섏㎏ 以??쒕ぉ ?ш린 */
  font-size:47px;
  /* 1踰?諛곕꼫 ?섏㎏ 以?以꾨넂??*/
  line-height:52px;
}
.hero__accentLine{
  color:var(--primary);
}
.hero__accent{color:var(--primary)}
.hero__desc{
  margin:0;
  color:var(--text-body-2);
  /* 1踰?諛곕꼫 蹂댁“ ?ㅻ챸 湲???ш린 */
  font-size:20px;
  /* 1踰?諛곕꼫 蹂댁“ ?ㅻ챸 以꾧컙寃?*/
  line-height:1.4;
  /* 以꾨컮轅???*/
  max-width:620px;
}
.hero__actions{display:flex; gap:16px; padding-top:0}
.hero__copy .heroPromo__social{
  padding-top:0;
}

/* Main banner slider (1踰?2踰??щ씪?대뱶 而⑦뀒?대꼫) */
.hero--banner{
  min-height:584px;
  display:flex;
  flex-direction:column;
  justify-content:space-between;
}
.heroSlideFrame{
  position:relative;
  width:100%;
  min-height:468px;
  display:flex;
  align-items:center;
  overflow:visible;
}
.heroSlideStack{
  position:relative;
  width:100%;
  min-height:468px;
}
.heroSlideMotion{
  width:100%;
}
.heroSlideMotion--layer{
  position:absolute;
  inset:0;
  display:flex;
  align-items:center;
  width:100%;
}
.heroSlideMotion--layer > *{
  width:100%;
}
.heroSlideMotion--current{
  opacity:1;
  transform:none;
}
.heroSlideMotion--in.heroSlideMotion--next{
  animation:heroSlideLayerInNext 1.02s cubic-bezier(.16,1,.3,1) both;
}
.heroSlideMotion--in.heroSlideMotion--prev{
  animation:heroSlideLayerInPrev 1.02s cubic-bezier(.16,1,.3,1) both;
}
.heroSlideMotion--out{
  pointer-events:none;
}
.heroSlideMotion--out.heroSlideMotion--next{
  animation:heroSlideLayerOutNext 1.02s cubic-bezier(.16,1,.3,1) both;
}
.heroSlideMotion--out.heroSlideMotion--prev{
  animation:heroSlideLayerOutPrev 1.02s cubic-bezier(.16,1,.3,1) both;
}
@keyframes heroSlideLayerInNext{
  0%{
    opacity:0;
    transform:translate3d(28px,0,0);
  }
  100%{
    opacity:1;
    transform:translate3d(0,0,0);
  }
}
@keyframes heroSlideLayerInPrev{
  0%{
    opacity:0;
    transform:translate3d(-28px,0,0);
  }
  100%{
    opacity:1;
    transform:translate3d(0,0,0);
  }
}
@keyframes heroSlideLayerOutNext{
  0%{
    opacity:1;
    transform:translate3d(0,0,0);
  }
  100%{
    opacity:0;
    transform:translate3d(-22px,0,0);
  }
}
@keyframes heroSlideLayerOutPrev{
  0%{
    opacity:1;
    transform:translate3d(0,0,0);
  }
  100%{
    opacity:0;
    transform:translate3d(22px,0,0);
  }
}
.heroPromo{
  position:relative;
  display:grid;
  grid-template-columns: minmax(0, 1fr) minmax(460px, .95fr);
  gap:72px;
  align-items:center;
  min-height:452px;
}
.heroPromo__left{
  display:flex;
  flex-direction:column;
  align-items:flex-start;
  gap:24px;
}
.heroPromo--scan{
  grid-template-columns:minmax(0, 1fr) minmax(480px, .98fr);
  gap:56px;
}
.heroPromo__left--scan{
  gap:22px;
}
.heroPromo__tag{
  display:inline-flex;
  align-items:center;
  padding:7px 14px;
  border-radius:9999px;
  border:1px solid #fbcfe8;
  background:rgba(255,255,255,.72);
  color:var(--accent-pink-strong);
  font-size:11px;
  letter-spacing:.14em;
  font-weight:700;
}
.heroPromo__tag--scan{
  border-color:var(--primary-soft-border);
  color:var(--primary);
}
.heroPromo__title{
  margin:0;
  /* 2踰?諛곕꼫 ?쒕ぉ ?ш린 */
  font-size:62px;
  /* 2踰?諛곕꼫 ?쒕ぉ 以꾨넂??*/
  line-height:1.08;
  letter-spacing:-.05em;
  color:var(--title);
  font-weight:800;
}
.heroPromo__title--scan{
  font-size:64px;
  line-height:1.02;
  letter-spacing:-.06em;
}
.heroPromo__line{
  display:inline-block;
  white-space:nowrap;
}
.heroPromo__accent{
  background:linear-gradient(90deg,var(--accent-pink),var(--accent-purple));
  -webkit-background-clip:text;
  background-clip:text;
  color:transparent;
}
.heroPromo__accent--blue{
  background:linear-gradient(90deg,var(--primary),var(--primary-hover));
  -webkit-background-clip:text;
  background-clip:text;
  color:transparent;
}
.heroPromo__desc{
  margin:0;
  max-width:760px;
  color:var(--text-body);
  /* 2踰?諛곕꼫 蹂댁“ ?ㅻ챸 ?ш린 */
  font-size:20px;
  /* 2踰?諛곕꼫 蹂댁“ ?ㅻ챸 以꾧컙寃?*/
  line-height:1.4;
}
.heroPromo__desc--scan{
  max-width:560px;
  color:var(--muted-3);
}
.heroPromo__actions{
  display:flex;
  gap:16px;
  padding-top:8px;
}
.heroPromo__actions--scan{
  padding-top:4px;
}
.heroPromo__btn{
  display:inline-flex;
  align-items:center;
  justify-content:center;
  border-radius:24px;
  /* 1踰?2踰?諛곕꼫 踰꾪듉 怨듯넻 ?ш린 */
  padding:18px 28px;
  font-size:16px;
  font-weight:700;
  border:0;
  cursor:pointer;
  min-width:220px;
  appearance:none;
  -webkit-appearance:none;
  overflow:hidden;
  box-sizing:border-box;
}
.heroPromo__btn--primary{
  color:var(--surface-base);
  background:linear-gradient(90deg,var(--accent-pink),var(--accent-purple));
  box-shadow:0 20px 25px -5px rgba(251,207,232,.9), 0 8px 10px -6px rgba(251,207,232,.9);
}
.heroPromo__btn--primaryBlue{
  background:linear-gradient(90deg,#38bdf8,var(--primary));
  box-shadow:0 20px 25px -5px rgba(56,189,248,.35), 0 8px 10px -6px rgba(37,99,235,.35);
}
.heroPromo__btn--ghost{
  background:var(--surface-base);
  color:var(--text-body);
  box-shadow:inset 0 0 0 1px var(--border-soft);
}
.heroPromo__social{
  display:flex;
  align-items:center;
  gap:20px;
  padding-top:16px;
}
.heroPromo__social--scan{
  padding-top:18px;
  margin-left:2px;
}
.heroPromo__social p{
  margin:0;
  font-size:14px;
  color:var(--text-body-2);
  font-weight:600;
}
.heroPromo__avatars{
  display:flex;
  align-items:center;
}
.heroPromo__avatars img,
.heroPromo__avatarsMore{
  width:40px;
  height:40px;
  border-radius:9999px;
  border:2px solid var(--surface-base);
  margin-right:-12px;
  object-fit:cover;
  background:var(--surface-base);
  box-shadow:0 4px 10px rgba(0,0,0,.06);
}
.heroPromo__avatarsMore{
  display:flex;
  align-items:center;
  justify-content:center;
  background:var(--tag-pink-bg);
  color:var(--accent-pink-strong);
  font-size:12px;
  font-weight:700;
}
.heroPromo__right{
  position:relative;
  min-height:420px;
  display:flex;
  align-items:center;
  justify-content:flex-end;
  padding-right:20px;
}
.heroPromo__right--scan{
  min-height:420px;
  justify-content:center;
  padding-right:0;
}

.scanScene{
  position:relative;
  width:min(100%, 560px);
  min-height:418px;
  transform:translate(26px, 10px);
}

.scanScene__glyphWrap{
  position:absolute;
  inset:26px 36px 52px 36px;
  display:flex;
  align-items:center;
  justify-content:center;
}

.scanScene__glyph{
  position:relative;
  z-index:2;
  color:var(--title);
  font-size:188px;
  line-height:1;
  font-weight:300;
  letter-spacing:-.08em;
}

.scanScene__gridLine{
  position:absolute;
}

.scanScene__gridLine--verticalLeft{
  left:78px;
  top:16px;
  bottom:28px;
  width:1px;
  border-left:1px dashed rgba(147,197,253,.5);
}

.scanScene__gridLine--verticalRight{
  right:64px;
  top:14px;
  bottom:36px;
  width:1px;
  border-left:1px dashed rgba(147,197,253,.45);
}

.scanScene__gridLine--top{
  left:112px;
  right:42px;
  top:62px;
  height:2px;
  background:linear-gradient(90deg,rgba(59,130,246,.55),rgba(59,130,246,.8),rgba(59,130,246,.55));
}

.scanScene__gridLine--mid{
  left:86px;
  right:72px;
  bottom:94px;
  height:1px;
  border-top:1px dashed rgba(147,197,253,.45);
}

.scanScene__laser{
  position:absolute;
  left:124px;
  right:78px;
  top:110px;
  height:2px;
  border-radius:999px;
  background:linear-gradient(90deg,rgba(59,130,246,0),rgba(59,130,246,.95) 14%,rgba(96,165,250,1) 50%,rgba(59,130,246,.95) 86%,rgba(59,130,246,0));
  box-shadow:0 0 10px rgba(59,130,246,.55), 0 0 18px rgba(96,165,250,.35);
  animation:scanLaserMove 3.8s ease-in-out infinite;
}

@keyframes scanLaserMove{
  0%, 100%{
    transform:translateY(-56px);
    opacity:.7;
  }
  50%{
    transform:translateY(92px);
    opacity:1;
  }
}

.scanScene__card{
  position:absolute;
  border-radius:14px;
  background:rgba(255,255,255,.94);
  border:1px solid rgba(219,234,254,.9);
  box-shadow:0 18px 30px rgba(15,23,42,.12);
}

.scanScene__card span{
  display:block;
  color:var(--muted-4);
  font-size:9px;
  line-height:14px;
  font-weight:800;
  letter-spacing:.12em;
}

.scanScene__card strong{
  display:block;
  color:var(--title);
  font-size:20px;
  line-height:1.15;
  font-weight:800;
}

.scanScene__card--engine{
  top:22px;
  left:12px;
  min-width:96px;
  padding:12px 16px;
}

.scanScene__card--engine strong{
  margin-top:4px;
  font-size:16px;
}

.scanScene__card--processing{
  top:-8px;
  right:28px;
  min-width:116px;
  padding:12px 14px;
  background:linear-gradient(180deg,var(--primary),#3b82f6);
  border-color:rgba(59,130,246,.95);
}

.scanScene__card--processing span,
.scanScene__card--processing strong{
  color:var(--surface-base);
}

.scanScene__card--processing strong{
  margin-top:4px;
  font-size:11px;
  line-height:1.2;
  font-weight:500;
}

.scanScene__card--match{
  left:0;
  bottom:62px;
  min-width:132px;
  padding:14px 18px 14px 46px;
}

.scanScene__card--match::before{
  content:'';
  position:absolute;
  left:16px;
  top:50%;
  width:18px;
  height:18px;
  border-radius:999px;
  border:2px solid #3b82f6;
  transform:translateY(-50%);
}

.scanScene__card--match::after{
  content:'';
  position:absolute;
  left:21px;
  top:50%;
  width:8px;
  height:4px;
  border-left:2px solid #3b82f6;
  border-bottom:2px solid #3b82f6;
  transform:translateY(-60%) rotate(-45deg);
}

.scanScene__card--match strong{
  font-size:16px;
}

.scanScene__card--probability{
  right:24px;
  bottom:74px;
  min-width:132px;
  padding:14px 16px;
}

.scanScene__metricRow{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:12px;
}

.scanScene__metricRow strong{
  font-size:16px;
  color:var(--primary);
}

.scanScene__metricRow--family{
  margin-top:10px;
  padding-top:10px;
  border-top:1px solid rgba(226,232,240,.9);
}

.scanScene__metricRow--family strong{
  color:var(--title);
  font-size:11px;
}
.heroPromo__card{
  width:380px;
  min-height:390px;
  border-radius:24px;
  padding:28px;
  background:linear-gradient(145deg,var(--surface-base),var(--info-soft));
  border:1px solid rgba(255,255,255,.4);
  box-shadow:0 25px 50px -12px rgba(0,0,0,.25);
  transform:rotate(2deg);
  animation:heroPromoCardFloat 6.4s ease-in-out infinite;
  will-change:transform;
}

@keyframes heroPromoCardFloat {
  0%, 100% {
    transform:translate3d(0, 0, 0) rotate(2deg);
  }
  50% {
    transform:translate3d(0, -14px, 0) rotate(0.5deg);
  }
}

@keyframes heroPromoBadgeFloat {
  0%, 100% {
    transform:translate3d(0, 0, 0);
  }
  50% {
    transform:translate3d(0, -10px, 0);
  }
}

@keyframes heroPromoChipFloat {
  0%, 100% {
    transform:translate3d(0, 0, 0) rotate(-12deg);
  }
  50% {
    transform:translate3d(0, -12px, 0) rotate(-9deg);
  }
}

@media (prefers-reduced-motion: reduce){
  .heroPromo__card,
  .heroPromo__floating{
    animation:none;
  }
}
.heroPromo__cardEyebrow{
  margin:0;
  font-size:10px;
  letter-spacing:.14em;
  color:var(--muted-4);
  font-weight:700;
}
.heroPromo__cardTitle{
  margin:8px 0 56px;
  font-size:28px;
  line-height:1.4;
  color:var(--title);
  white-space:nowrap;
}
.heroPromo__handword{
  margin:0 0 84px;
  text-align:center;
  font-size:52px;
  line-height:1.2;
  font-family:'Nanum Pen Script', cursive;
  color:var(--text-body-strong);
}
.heroPromo__cardFooter{
  border-top:1px solid var(--border-subtle);
  padding-top:16px;
  display:flex;
  align-items:center;
  gap:8px;
  font-size:11px;
  color:var(--text-body-2);
}
.heroPromo__cardDot{
  width:8px;
  height:8px;
  border-radius:9999px;
  background:var(--accent-pink);
}
.heroPromo__version{
  margin-left:auto;
  font-size:8px;
  color:var(--muted-4);
}
.heroPromo__floating{
  position:absolute;
  background:rgba(255,255,255,.82);
  border:1px solid rgba(255,255,255,.4);
  box-shadow:0 20px 25px -5px rgba(0,0,0,.1), 0 8px 10px -6px rgba(0,0,0,.1);
  animation:heroPromoBadgeFloat 5.6s ease-in-out infinite;
  will-change:transform;
}
.heroPromo__floating--badge{
  top:28px;
  right:-4px;
  border-radius:9999px;
  padding:14px 16px;
  font-size:12px;
  font-weight:800;
  color:var(--text-body-2);
  white-space:nowrap;
}
.heroPromo__floating--chip{
  left:-12px;
  top:52%;
  transform:rotate(-12deg);
  border-radius:8px;
  padding:8px 12px;
  font-size:11px;
  font-weight:600;
  color:var(--text-body-2);
  white-space:nowrap;
  animation-name:heroPromoChipFloat;
  animation-duration:6.9s;
  animation-delay:-1.8s;
}
.heroBanner__dots{
  margin-top:20px;
  padding-bottom:4px;
  display:flex;
  justify-content:center;
  align-items:center;
  gap:12px;
}
.heroBanner__arrow{
  width:34px;
  height:34px;
  border:1px solid rgba(148,163,184,.32);
  border-radius:999px;
  background:rgba(255,255,255,.78);
  color:var(--muted-3);
  display:grid;
  place-items:center;
  font-size:22px;
  line-height:1;
  cursor:pointer;
  transition:background .18s ease, color .18s ease, border-color .18s ease;
}
.heroBanner__arrow:hover{
  background:var(--surface-base);
  color:var(--primary-hover);
  border-color:rgba(37,99,235,.32);
}
.heroBanner__dot{
  width:8px;
  height:8px;
  border:0;
  border-radius:9999px;
  background:var(--border-strong);
  cursor:pointer;
}
.heroBanner__dot--active{
  width:22px;
  background:var(--accent-purple);
}

/* Preview cards (1踰??щ씪?대뱶 ?곗륫 移대뱶) */
.hero__cards{
  position:relative;
  flex:0 0 470px;
  width:470px;
  max-width:470px;
  height:440px;
  margin-left:auto;
  transform:none;
}
.hero__grid--existing .hero__cards{
  position:absolute;
  /* ?곗륫 移대뱶 臾띠쓬 ?꾩튂 ?듭떖媛? ?뚯닔?쇱닔濡????ㅻⅨ履??대룞 */
  right:-12px;
  top:50%;
  transform:translateY(-50%);
  margin-left:0;
}
.pv{
  text-decoration:none;
  background:var(--surface-base);
  border:1px solid var(--card-border);
  border-radius:var(--r-12);
  box-shadow:var(--shadow-1);
  padding:21px;
  display:flex;
  flex-direction:column;
  gap:12px;
  min-height:126px;
  transition:
    transform .2s ease,
    border-color .2s ease,
    box-shadow .2s ease,
    background .2s ease;
}
.pv__top{display:flex; align-items:center; justify-content:space-between; gap:12px}
.pv__name{font-size:18px; color:var(--text-body-2); font-weight:600}
.pv__tag{font-size:12px; color:var(--muted-2)}
.pv__sample{
  font-size:14px;
  line-height:20px;
  color:var(--text);
}
.pv--dim{opacity:.5}
.pv__skeleton{height:24px; background:var(--bg-soft); border-radius:8px; width:145px}
.pv--glass{
  position:relative;
  width:300px;
  min-height:114px;
  padding:18px 22px;
  flex:0 0 auto;
  border-radius:8px;
  background:
    linear-gradient(180deg, rgba(255,255,255,.56), rgba(255,255,255,.2)),
    linear-gradient(135deg, rgba(255,255,255,.34), rgba(255,255,255,.08));
  border-color:rgba(255,255,255,.72);
  box-shadow:
    0 18px 38px rgba(15,23,42,.13),
    inset 0 1px 0 rgba(255,255,255,.72),
    inset 0 -1px 0 rgba(255,255,255,.28);
  backdrop-filter:blur(18px) saturate(160%);
  -webkit-backdrop-filter:blur(18px) saturate(160%);
  transition:
    transform .18s ease-out,
    opacity .18s ease-out,
    border-color .2s ease,
    box-shadow .2s ease,
    background .2s ease;
  will-change:transform;
  transform-origin:center center;
  scroll-snap-align:center;
}
.pv--glass::after{
  content:'';
  position:absolute;
  left:18px;
  right:42px;
  bottom:18px;
  height:15px;
  border-radius:999px;
  background:rgba(22,42,68,.22);
  filter:blur(5px);
  opacity:.7;
  pointer-events:none;
}
.pv--glass:hover{
  border-color:rgba(255,255,255,.9);
  background:
    linear-gradient(180deg, rgba(255,255,255,.56), rgba(255,255,255,.24)),
    linear-gradient(135deg, rgba(255,255,255,.38), rgba(255,255,255,.12));
  box-shadow:
    0 22px 44px rgba(15,23,42,.16),
    inset 0 1px 0 rgba(255,255,255,.78),
    inset 0 -1px 0 rgba(255,255,255,.3);
}
.pv--glass .pv__name{
  color:#0f172a;
  font-size:25px;
  line-height:31px;
  font-weight:500;
}
.pv--glass .pv__sample{
  position:relative;
  z-index:1;
  max-width:210px;
  color:rgba(15,23,42,.7);
  font-size:14px;
  line-height:19px;
  filter:blur(1.2px);
  transition:filter .18s ease, color .18s ease;
}
.pv--focus{
  border-color:rgba(255,255,255,.92);
  background:
    linear-gradient(180deg, rgba(255,255,255,.68), rgba(255,255,255,.26)),
    linear-gradient(135deg, rgba(255,255,255,.44), rgba(255,255,255,.12));
  box-shadow:
    0 34px 66px rgba(15,23,42,.22),
    0 8px 20px rgba(37,99,235,.12),
    inset 0 1px 0 rgba(255,255,255,.86),
    inset 0 -1px 0 rgba(255,255,255,.36);
}
.pv--focus .pv__sample{
  color:rgba(15,23,42,.78);
  filter:none;
}
.heroPreviewScroller{
  position:absolute;
  inset:0;
  display:flex;
  flex-direction:column;
  gap:20px;
  overflow-y:auto;
  overscroll-behavior:contain;
  scroll-snap-type:y mandatory;
  scroll-behavior:smooth;
  padding:92px 24px 192px 96px;
  perspective:760px;
  outline:none;
  scrollbar-width:none;
  -webkit-mask-image:linear-gradient(180deg, transparent 0, #000 8%, #000 88%, transparent 100%);
  mask-image:linear-gradient(180deg, transparent 0, #000 8%, #000 88%, transparent 100%);
}
.heroPreviewScroller::-webkit-scrollbar{
  display:none;
}
.heroPreviewScroller:focus-visible .pv--glass:first-child{
  box-shadow:
    0 22px 44px rgba(15,23,42,.16),
    0 0 0 3px rgba(96,165,250,.18),
    inset 0 1px 0 rgba(255,255,255,.78),
    inset 0 -1px 0 rgba(255,255,255,.3);
}

/* Pills */
.pill{
  background:var(--pill-bg);
  color:var(--primary);
  border-radius:8px;
  padding:2px 8px;
  font-size:12px;
  line-height:16px;
}

/* Font cards rows */
.cards{display:grid; gap:24px}
.cards--4{grid-template-columns:repeat(4, minmax(0,1fr))}
.featureFontsGrid{
  display:grid;
  grid-template-columns:repeat(4, minmax(0, 1fr));
  gap:30px;
}

.featureFontsGrid--threeUp{
  grid-template-columns:repeat(3, minmax(0, 1fr));
  gap:34px 30px;
}

.featureFontsGrid--animated{
  animation-duration:.38s;
  animation-timing-function:cubic-bezier(.22,1,.36,1);
  animation-fill-mode:both;
}

.featureFontsGrid--animated .featureFontCard{
  opacity:0;
  animation:featureFontCardCascade .46s cubic-bezier(.22,1,.36,1) forwards;
}

.featureFontsGrid--animated .featureFontCard:nth-child(1){
  animation-delay:.04s;
}

.featureFontsGrid--animated .featureFontCard:nth-child(2){
  animation-delay:.11s;
}

.featureFontsGrid--animated .featureFontCard:nth-child(3){
  animation-delay:.18s;
}

.featureFontsGrid--next{
  animation-name:featureFontsSlideNext;
}

.featureFontsGrid--prev{
  animation-name:featureFontsSlidePrev;
}

@keyframes featureFontsSlideNext{
  from{
    opacity:0;
    transform:translateX(28px) scale(.985);
  }
  to{
    opacity:1;
    transform:translateX(0) scale(1);
  }
}

@keyframes featureFontsSlidePrev{
  from{
    opacity:0;
    transform:translateX(-28px) scale(.985);
  }
  to{
    opacity:1;
    transform:translateX(0) scale(1);
  }
}

@keyframes featureFontCardCascade{
  from{
    opacity:0;
    transform:translateY(18px) scale(.985);
    filter:blur(4px);
  }
  to{
    opacity:1;
    transform:translateY(0) scale(1);
    filter:blur(0);
  }
}

.featureFontsToolbar{
  display:flex;
  align-items:center;
  gap:16px;
}

.featureFontsPager{
  display:flex;
  align-items:center;
  gap:10px;
}

.featureFontsPager__button{
  width:38px;
  height:38px;
  border:1px solid #d6dfeb;
  border-radius:999px;
  background:var(--surface-base);
  color:#274690;
  font-size:22px;
  line-height:1;
  display:grid;
  place-items:center;
  cursor:pointer;
  box-shadow:0 6px 14px rgba(15,23,42,.06);
}

.featureFontsPager__status{
  min-width:52px;
  text-align:center;
  color:var(--muted-3);
  font-size:13px;
  font-weight:700;
}

.featureFontCard{
  position:relative;
  overflow:hidden;
  border:1px solid #dbe3f0;
  border-radius:16px;
  background:var(--surface-base);
  box-shadow:0 14px 34px rgba(15,23,42,.06);
}

.featureFontCard__favorite{
  position:absolute;
  top:24px;
  right:24px;
  z-index:2;
  width:48px;
  height:48px;
  border:1px solid #dbe3f0;
  border-radius:999px;
  background:rgba(255,255,255,.94);
  color:var(--text-body-2);
  font-size:24px;
  line-height:1;
  display:grid;
  place-items:center;
  cursor:pointer;
  transition:transform .18s ease, border-color .18s ease, background-color .18s ease, color .18s ease, box-shadow .18s ease;
}

.featureFontCard__favoriteIcon{
  width:22px;
  height:22px;
  display:block;
  transition:transform .18s ease, fill .18s ease, stroke .18s ease;
}

.featureFontCard__downloadIcon{
  width:20px;
  height:20px;
  display:block;
}

.featureFontCard__favorite:hover{
  transform:translateY(-1px) scale(1.03);
  border-color:#fecdd3;
  color:#e11d48;
  box-shadow:0 10px 20px rgba(225,29,72,.12);
}

.featureFontCard__favorite.is-active{
  border-color:#fecdd3;
  background:var(--danger-soft);
  color:#e11d48;
  box-shadow:0 10px 22px rgba(225,29,72,.14);
}

.featureFontCard__favorite.is-active .featureFontCard__favoriteIcon{
  transform:scale(1.08);
}

.featureFontCard__favorite.is-active .featureFontCard__favoriteIcon path{
  fill:currentColor;
}

.featureFontCard__preview{
  min-height:292px;
  padding:26px 24px 22px;
  border-bottom:1px solid #edf2f7;
  background:linear-gradient(180deg,var(--surface-base) 0%, #fcfdff 100%);
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
  text-align:center;
}

.featureFontCard__previewInner{
  width:100%;
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
}

.featureFontCard__preview--multiline .featureFontCard__previewInner{
  transform:translateY(14px);
}

.featureFontCard__glyph{
  color:var(--text-heading);
  font-size:88px;
  line-height:.92;
  font-weight:800;
  letter-spacing:-.08em;
  display:flex;
  align-items:flex-end;
  gap:10px;
}

.featureFontCard__glyph span{
  display:block;
}

.featureFontCard__glyph--wordmark{
  display:block;
  font-size:62px;
  line-height:1.02;
  letter-spacing:-.04em;
  text-align:center;
  word-break:keep-all;
}

.featureFontCard__glyph--wordmark span{
  display:block;
}

.featureFontCard__headline{
  margin:16px 0 0;
  color:var(--text-body-2);
  font-size:16px;
  line-height:1.45;
  font-weight:700;
}

.featureFontCard__headline--large{
  font-size:18px;
  line-height:1.5;
}

.featureFontCard__copy{
  margin:10px 0 0;
  color:var(--muted-3);
  font-size:14px;
  line-height:1.6;
  white-space:pre-line;
}

.featureFontCard__body{
  padding:22px 22px 22px;
  display:flex;
  flex-direction:column;
  gap:14px;
}

.featureFontCard__meta{
  display:flex;
  align-items:flex-start;
  justify-content:space-between;
  gap:16px;
}

.featureFontCard__meta strong{
  display:block;
  margin:0;
  color:var(--text-heading);
  font-size:18px;
  line-height:1.25;
  font-weight:900;
  letter-spacing:-.04em;
}

.featureFontCard__meta p{
  margin:6px 0 0;
  color:var(--text-body-2);
  font-size:14px;
  line-height:1.45;
}

.featureFontCard__meta span{
  flex:0 0 auto;
  min-height:36px;
  padding:0 16px;
  border:1px solid var(--info-soft);
  border-radius:12px;
  background:var(--primary-soft);
  color:#0057d8;
  font-size:12px;
  line-height:1;
  font-weight:800;
  display:inline-flex;
  align-items:center;
  justify-content:center;
  letter-spacing:.08em;
}

.featureFontCard__actions{
  display:flex;
  justify-content:flex-end;
}

.featureFontCard__download,
.featureFontCard__download--icon{
  min-height:48px;
  border-radius:12px;
  font-size:14px;
  font-weight:800;
  display:flex;
  align-items:center;
  justify-content:center;
  text-decoration:none;
}

.featureFontCard__download{
  border:1px solid #d5e1f2;
  background:var(--surface-base);
  color:#0057d8;
  box-shadow:none;
}

.featureFontCard__download--icon{
  width:48px;
  min-width:48px;
  padding:0;
  font-size:18px;
  box-shadow:none;
}

.section--popularFontsDuplicate,
.recommendationShowcaseDuplicate{
  display:none;
}

.recommendationShowcase{
  padding-top:24px;
}

.recommendationShowcase__head{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:24px;
  margin-bottom:24px;
  padding:0;
  border:0;
  border-radius:0;
  background:transparent;
  box-shadow:none;
}

.recommendationShowcase__eyebrow{
  margin:0 0 6px;
  color:#2f5fcd;
  font-size:11px;
  line-height:1;
  font-weight:800;
  letter-spacing:.08em;
}

.recommendationShowcase__title{
  margin:0;
  color:#121212;
  font-size:24px;
  line-height:32px;
  font-weight:700;
  letter-spacing:-.02em;
}

.recommendationShowcase__desc{
  margin:4px 0 0;
  color:#7a8495;
  font-size:16px;
  line-height:24px;
  font-weight:500;
}

.recommendationShowcase__actions{
  display:flex;
  align-items:center;
  gap:10px;
  padding-top:0;
}

.recommendationShowcase__actionButton{
  min-width:114px;
  height:46px;
  padding:0 18px;
  border:1px solid #dbe4ff;
  border-radius:999px;
  background:#fff;
  color:#3159d6;
  font-size:14px;
  line-height:1;
  font-weight:700;
  cursor:pointer;
  box-shadow:0 8px 18px rgba(49,89,214,.08);
  transition:border-color .2s ease, transform .2s ease, box-shadow .2s ease;
}

.recommendationShowcase__actionButton:hover{
  border-color:#b9caff;
  transform:translateY(-1px);
  box-shadow:0 12px 24px rgba(49,89,214,.12);
}

.recommendationShowcase__navButton{
  width:46px;
  height:46px;
  border:1px solid #dbe4ff;
  border-radius:999px;
  background:#fff;
  color:#3159d6;
  font-size:20px;
  line-height:1;
  display:grid;
  place-items:center;
  cursor:pointer;
  box-shadow:0 8px 18px rgba(49,89,214,.08);
  transition:border-color .2s ease, transform .2s ease, box-shadow .2s ease;
}

.recommendationShowcase__navButton:hover{
  border-color:#b9caff;
  transform:translateY(-1px);
  box-shadow:0 12px 24px rgba(49,89,214,.12);
}

.recommendationDeck{
  margin-top:0;
}

.recommendationDeck__controls{
  margin-top:8px;
  display:flex;
  justify-content:center;
}

.recommendationDeck__nav{
  display:flex;
  align-items:center;
  justify-content:center;
  gap:18px;
}

.recommendationDeck__stage{
  position:relative;
  height:620px;
  overflow:visible;
  isolation:isolate;
  perspective:1800px;
  perspective-origin:center center;
}

.recommendationCard{
  position:absolute;
  left:50%;
  top:50%;
  width:276px;
  height:440px;
  --recommendation-card-active-x:-50%;
  --recommendation-card-active-y:-53%;
  --recommendation-card-active-z:120px;
  --recommendation-card-active-scale:1.08;
  transform-style:preserve-3d;
  transform-origin:center center;
  transition:transform .78s cubic-bezier(.22,1,.36,1), opacity .52s ease, z-index .01s linear;
  will-change:transform, opacity;
}

.recommendationCard.is-jump-reset{
  transition:none;
  animation:none;
}

.recommendationCard.is-jump-reset .recommendationCard__inner,
.recommendationCard.is-jump-reset .recommendationCard__face{
  transition:none;
}

.recommendationCard--offset--2{
  transform:translate(-138%, -48%) rotate(-8deg) scale(.88);
  z-index:1;
  opacity:.56;
}

.recommendationCard--offset--1{
  transform:translate(-98%, -49%) rotate(-5deg) scale(.93);
  z-index:2;
  opacity:.78;
}

.recommendationCard--offset-0{
  transform:translate3d(
    var(--recommendation-card-active-x),
    var(--recommendation-card-active-y),
    var(--recommendation-card-active-z)
  ) rotate(0deg) scale(var(--recommendation-card-active-scale));
  z-index:5;
  opacity:1;
}

.recommendationCard--offset-1{
  transform:translate(-2%, -49%) rotate(5deg) scale(.93);
  z-index:2;
  opacity:.78;
}

.recommendationCard--offset-2{
  transform:translate(38%, -48%) rotate(8deg) scale(.88);
  z-index:1;
  opacity:.56;
}

.recommendationCard__inner{
  position:relative;
  width:100%;
  height:100%;
  transform-style:preserve-3d;
  transition:transform .82s cubic-bezier(.22,1,.36,1);
}

.recommendationCard__motion{
  position:relative;
  width:100%;
  height:100%;
  transform-style:preserve-3d;
  transition:transform .52s ease;
}

.recommendationCard.is-active .recommendationCard__inner{
  transform:rotateY(180deg);
}

.recommendationCard--offset-0 .recommendationCard__motion{
  animation:recommendationCardFloat 4.6s cubic-bezier(.42,0,.22,1) infinite;
}

.recommendationCard__face{
  position:absolute;
  inset:0;
  backface-visibility:hidden;
  -webkit-backface-visibility:hidden;
  border:1px solid rgba(15,23,42,.12);
  border-radius:24px;
  background:#fff;
  box-shadow:0 20px 42px rgba(15,23,42,.08);
  overflow:hidden;
  transition:opacity .28s ease, box-shadow .35s ease;
}

.recommendationCard--offset-0 .recommendationCard__face{
  box-shadow:0 30px 64px rgba(15,23,42,.16);
}

.recommendationCard__face--front{
  display:flex;
  flex-direction:column;
  transform:rotateY(180deg);
  opacity:0;
}

.recommendationCard__face--back{
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:space-between;
  padding:26px 22px 18px;
  background:
    linear-gradient(180deg, rgba(255,255,255,.98), rgba(247,249,255,.98)),
    linear-gradient(135deg, #ffffff, #eef4ff 65%, #dde8ff);
  opacity:1;
}

.recommendationCard.is-active .recommendationCard__face--front{
  opacity:1;
}

.recommendationCard.is-active .recommendationCard__face--back{
  opacity:0;
}

.recommendationCard__backLabel{
  width:100%;
  color:#2f5fcd;
  font-size:12px;
  line-height:1;
  font-weight:800;
  letter-spacing:.14em;
}

.recommendationCard__backGlyph{
  color:#1f2937;
  font-size:96px;
  line-height:1;
  font-weight:700;
}

.recommendationCard__media{
  height:72%;
  background:
    radial-gradient(circle at 20% 20%, rgba(255,255,255,.7), transparent 28%),
    linear-gradient(135deg, #edf3ff 0%, #f7faff 42%, #ffffff 100%);
}

.recommendationCard__content{
  position:absolute;
  inset:22px 20px 74px;
  display:flex;
  flex-direction:column;
  justify-content:flex-start;
  gap:12px;
}

.recommendationCard__eyebrow{
  display:inline-flex;
  align-self:flex-start;
  min-height:28px;
  padding:0 11px;
  align-items:center;
  border-radius:999px;
  border:1px solid rgba(49,89,214,.14);
  background:rgba(255,255,255,.88);
  color:#3159d6;
  font-size:11px;
  line-height:1;
  font-weight:800;
  letter-spacing:.08em;
}

.recommendationCard__name{
  margin:0;
  color:#111827;
  font-size:24px;
  line-height:1.18;
  font-weight:800;
  letter-spacing:-.04em;
  word-break:keep-all;
  overflow-wrap:normal;
}

.recommendationCard__sample{
  margin:0;
  color:#111827;
  font-size:26px;
  line-height:1.32;
  white-space:normal;
  word-break:keep-all;
  overflow-wrap:break-word;
  line-break:loose;
}

.recommendationCard__description{
  margin:0;
  color:#4b5563;
  font-size:13px;
  line-height:1.65;
  white-space:normal;
  word-break:keep-all;
  overflow-wrap:break-word;
  line-break:loose;
}

.recommendationCard__footer{
  position:absolute;
  left:0;
  right:0;
  bottom:0;
  min-height:58px;
  padding:0 18px;
  border-top:1px solid #edf0f5;
  color:#3159d6;
  font-size:12px;
  line-height:1;
  font-weight:700;
  letter-spacing:.04em;
  display:flex;
  align-items:center;
  justify-content:space-between;
  background:rgba(255,255,255,.96);
}

@keyframes recommendationCardFloat{
  0%,
  100%{
    transform:translateY(0);
  }
  50%{
    transform:translateY(-10px);
  }
}

.recommendationDeck__dots{
  margin-top:0;
  display:flex;
  justify-content:center;
  align-items:center;
  gap:10px;
}

.recommendationDeck__dot{
  width:8px;
  height:8px;
  border:0;
  border-radius:999px;
  background:#d7dfea;
  cursor:pointer;
}

.recommendationDeck__dot.is-active{
  background:#3159d6;
}

@media (max-width: 960px){
  .recommendationShowcase__head{
    flex-direction:column;
    align-items:flex-start;
  }

  .recommendationShowcase__title{
    font-size:24px;
    line-height:32px;
  }

  .recommendationShowcase__desc{
    font-size:16px;
    line-height:24px;
  }

  .recommendationShowcase__actions{
    width:100%;
    justify-content:flex-end;
  }

  .recommendationDeck__stage{
    height:560px;
  }

  .recommendationCard{
    width:244px;
    height:398px;
    --recommendation-card-active-y:-52%;
    --recommendation-card-active-z:96px;
    --recommendation-card-active-scale:1.05;
  }

  .recommendationCard--offset--2{
    transform:translate(-126%, -48%) rotate(-6deg) scale(.86);
  }

  .recommendationCard--offset--1{
    transform:translate(-92%, -49%) rotate(-4deg) scale(.91);
  }

  .recommendationCard--offset-1{
    transform:translate(0%, -49%) rotate(4deg) scale(.91);
  }

  .recommendationCard--offset-2{
    transform:translate(26%, -48%) rotate(6deg) scale(.86);
  }
}

@media (max-width: 720px){
  .recommendationShowcase__actions{
    justify-content:flex-start;
  }

  .recommendationShowcase__actionButton{
    height:42px;
    padding:0 16px;
    font-size:13px;
  }

  .recommendationShowcase__navButton{
    width:42px;
    height:42px;
    font-size:18px;
  }

  .recommendationDeck__nav{
    gap:14px;
  }

  .recommendationDeck__stage{
    height:460px;
  }

  .recommendationCard{
    width:212px;
    height:344px;
    --recommendation-card-active-y:-51%;
    --recommendation-card-active-z:72px;
    --recommendation-card-active-scale:1.03;
  }

  .recommendationCard--offset--2,
  .recommendationCard--offset-2{
    opacity:0;
    pointer-events:none;
  }

  .recommendationCard--offset--1{
    transform:translate(-86%, -49%) rotate(-5deg) scale(.88);
  }

  .recommendationCard--offset-1{
    transform:translate(-14%, -49%) rotate(5deg) scale(.88);
  }

  .recommendationCard__backGlyph{
    font-size:78px;
  }

  .recommendationCard__name{
    font-size:19px;
  }

  .recommendationCard__sample{
    font-size:20px;
  }
}

@media (prefers-reduced-motion: reduce){
  .recommendationCard--offset-0 .recommendationCard__motion{
    animation:none;
  }
}

.font-card{
  position:relative;
  z-index:1;
  background:var(--surface-base);
  border:1px solid #e6e6e6;
  border-radius:var(--r-12);
  padding:25px;
  box-shadow:var(--shadow-1);
}
.font-card--active{
  border:2px solid #d3e8ff;
  padding:26px;
}
.font-card--link{
  display:block;
  text-decoration:none;
  color:inherit;
  cursor:pointer;
  transition:border-color .15s ease, box-shadow .15s ease, transform .15s ease;
}
.font-card--link:hover{
  z-index:60;
  border-color:#d3e8ff;
  box-shadow:0 0 0 1px rgba(211,232,255,0.9), 0 8px 16px rgba(15,23,42,0.08);
  transform:translateY(-1px);
}
.font-card--link:hover .pill{
  background:var(--pill-bg);
  color:var(--primary);
}
.font-card__top{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:12px;
  margin-bottom:16px;
}
.font-card__name{color:var(--muted); font-size:14px; font-weight:500}
.font-card__sample{
  font-size:30px;
  line-height:36px;
  color:var(--text);
  overflow:hidden;
  text-overflow:ellipsis;
  white-space:nowrap;
}
.font-card__popover{
  position:absolute;
  left:50%;
  bottom:calc(100% + 12px);
  z-index:40;
  width:min(300px, calc(100vw - 48px));
  padding:18px;
  border:1px solid rgba(226,232,240,.9);
  border-radius:12px;
  background:var(--surface-base);
  box-shadow:0 18px 40px rgba(15,23,42,.14);
  color:var(--text-body-strong);
  opacity:0;
  visibility:hidden;
  transform:translate(-50%, 4px);
  transition:
    opacity .18s ease .6s,
    transform .18s ease .6s,
    visibility 0s linear .78s;
}
.font-card__popover::before{
  content:'';
  position:absolute;
  left:50%;
  bottom:-7px;
  width:12px;
  height:12px;
  border-right:1px solid rgba(226,232,240,.9);
  border-bottom:1px solid rgba(226,232,240,.9);
  background:var(--surface-base);
  transform:translateX(-50%) rotate(45deg);
}
.font-card--link:hover .font-card__popover,
.font-card--link:focus-visible .font-card__popover{
  opacity:1;
  visibility:visible;
  transform:translate(-50%, 0);
  transition-delay:.6s,.6s,0s;
}
.font-card__popover strong{
  display:block;
  margin-bottom:10px;
  color:var(--title);
  font-size:14px;
  line-height:20px;
  font-weight:800;
}
.font-card__popoverSample{
  min-height:58px;
  display:flex;
  align-items:center;
  padding:12px 14px;
  border-radius:8px;
  background:var(--surface-subtle);
  color:var(--text-body-strong);
  font-size:24px;
  line-height:30px;
  white-space:nowrap;
  overflow:hidden;
  text-overflow:ellipsis;
}
.font-card__popover p{
  margin:12px 0 0;
  color:var(--muted-3);
  font-size:13px;
  line-height:20px;
  word-break:keep-all;
}

.font-card__popover--showcase{
  width:min(430px, calc(100vw - 48px));
  padding:14px;
  border:0;
  border-radius:2px;
  background:var(--border-emphasis);
  box-shadow:0 28px 70px rgba(15,23,42,.22);
}

.font-card__popover--showcase::before{
  background:var(--border-emphasis);
  border-color:var(--border-emphasis);
}

.font-card__popoverVisual{
  min-height:300px;
  aspect-ratio:1.49;
}

.font-card__popoverVisual.popularFontCard__visual--paper .popularFontCard__quote{
  font-size:18px;
}

.font-card__popoverVisual.popularFontCard__visual--sunrise .popularFontCard__quote{
  font-size:27px;
}

.font-card__popoverVisual.popularFontCard__visual--mist .popularFontCard__quote{
  font-size:34px;
}

.font-card__popoverVisual.popularFontCard__visual--gradient .popularFontCard__quote{
  font-size:40px;
}

.font-card__popoverShowcaseText{
  display:grid;
  grid-template-columns:1fr;
  gap:4px;
  padding:14px 0 0;
}

.font-card__popoverShowcaseText strong{
  margin:0;
  color:#111;
  font-size:20px;
  line-height:26px;
  font-weight:300;
  text-transform:lowercase;
}

.font-card__popoverShowcaseText p{
  margin:0;
  color:#111;
  font-size:11px;
  line-height:16px;
  font-weight:300;
}

.font-card__popoverShowcaseText span{
  justify-self:end;
  color:#111;
  font-size:11px;
  line-height:16px;
  font-weight:300;
}

.popularFontCard{
  min-height:360px;
  display:flex;
  flex-direction:column;
  gap:0;
  padding:10px;
  border-radius:0;
  background:var(--border-emphasis);
  color:var(--text-strong);
  text-decoration:none;
  box-shadow:var(--shadow-1);
}

.popularFontCard__visual{
  min-height:228px;
  display:flex;
  align-items:center;
  justify-content:center;
  padding:24px;
  overflow:hidden;
  position:relative;
}

.popularFontCard__visual--paper{
  background:
    radial-gradient(circle at 12% 18%, rgba(94,79,63,.22) 0 2px, transparent 3px),
    radial-gradient(circle at 24% 30%, rgba(94,79,63,.16) 0 28px, transparent 29px),
    linear-gradient(135deg,#dfd7cb,#f1eee7 42%,#c7bcae);
}

.popularFontCard__visual--paper::before{
  content:'';
  position:absolute;
  width:62%;
  height:48%;
  background:#f7f3eb;
  box-shadow:0 16px 18px rgba(62,45,29,.22);
  transform:rotate(-1deg);
}

.popularFontCard__visual--sunrise{
  background:linear-gradient(180deg,#1594d8 0%,#66b88c 47%,#d5f136 100%);
}

.popularFontCard__visual--mist{
  background:
    radial-gradient(circle at 66% 30%,rgba(245,160,84,.5),transparent 28%),
    radial-gradient(circle at 28% 20%,rgba(115,90,135,.55),transparent 35%),
    linear-gradient(135deg,#c4b2c6,#e5c7a1 48%,#8a8099);
}

.popularFontCard__visual--mist::after{
  content:'';
  position:absolute;
  right:0;
  bottom:0;
  width:44%;
  height:45%;
  opacity:.28;
  background:
    linear-gradient(32deg,transparent 45%,#151515 46% 48%,transparent 49%) 0 30%/100% 100% no-repeat,
    linear-gradient(-26deg,transparent 44%,#151515 45% 47%,transparent 48%) 20% 0/100% 100% no-repeat;
}

.popularFontCard__visual--gradient{
  background:linear-gradient(180deg,rgba(245,57,126,.87),rgba(61,145,255,.87));
}

.popularFontCard__quote{
  position:relative;
  z-index:1;
  display:flex;
  flex-direction:column;
  align-items:center;
  gap:2px;
  color:#111;
  text-align:center;
  font-size:30px;
  line-height:1.25;
  word-break:keep-all;
}

.popularFontCard__visual--paper .popularFontCard__quote{
  width:58%;
  padding:26px 10px;
  font-size:24px;
}

.popularFontCard__visual--sunrise .popularFontCard__quote{
  color:#3d2323;
  font-size:30px;
  font-weight:400;
}

.popularFontCard__visual--mist .popularFontCard__quote{
  font-size:38px;
}

.popularFontCard__visual--gradient .popularFontCard__quote{
  color:var(--surface-base);
  font-size:42px;
}

.popularFontCard__body{
  min-height:112px;
  display:grid;
  grid-template-rows:auto 1fr auto;
  gap:2px;
  padding:12px 0 0;
}

.popularFontCard__body h3{
  margin:0;
  color:#111;
  font-size:22px;
  line-height:28px;
  font-weight:300;
  text-transform:lowercase;
}

.popularFontCard__body p{
  margin:0;
  color:#111;
  font-size:12px;
  line-height:18px;
  font-weight:300;
  word-break:keep-all;
}

.popularFontCard__body span{
  justify-self:end;
  color:#111;
  font-size:12px;
  line-height:18px;
  font-weight:300;
}

/* How it works */
.howto{
  background:
    radial-gradient(circle at 18% 18%, rgba(59,130,246,.06), transparent 24%),
    linear-gradient(180deg, #fcfdff 0%, #f8fbff 100%);
  padding:116px 0;
  position:relative;
  overflow:hidden;
  border-top:1px solid rgba(229,231,235,.0);
}
.howto__blur{
  position:absolute;
  left:-56px;
  top:64px;
  width:360px;
  height:360px;
  border-radius:999px;
  background:rgba(59,130,246,.06);
  filter:blur(48px);
}
.howto__inner{text-align:center; position:relative}
.howto__eyebrow{
  margin:0 0 8px;
  font-size:13px;
  letter-spacing:.08em;
  text-transform:uppercase;
  color:#2563eb;
  font-weight:800;
}
.howto__title{
  margin:0 0 68px;
  font-size:42px;
  line-height:1.1;
  font-weight:700;
  color:var(--title);
}
.howtoFlow{
  display:grid;
  grid-template-columns:320px minmax(0, 1fr);
  gap:48px;
  align-items:start;
  text-align:left;
}
.howtoSteps{
  position:relative;
  display:grid;
  gap:38px;
  padding:40px 0 28px;
}
.howtoSteps__line{
  position:absolute;
  left:23px;
  top:56px;
  bottom:56px;
  width:1px;
  background:linear-gradient(180deg, rgba(37,99,235,.28), rgba(37,99,235,.08));
}
.howtoStep{
  position:relative;
  display:grid;
  grid-template-columns:58px 1fr;
  gap:20px;
  align-items:center;
  z-index:1;
}
.howtoStep__icon{
  width:58px;
  height:58px;
  border-radius:16px;
  background:#fff;
  border:1px solid rgba(37,99,235,.10);
  box-shadow:0 8px 20px rgba(37,99,235,.08);
  display:grid;
  place-items:center;
}
.howtoStep__icon svg{
  width:28px;
  height:28px;
  fill:none;
  stroke:#2563eb;
  stroke-width:1.8;
  stroke-linecap:round;
  stroke-linejoin:round;
}
.howtoStep__number{
  display:block;
  margin-bottom:8px;
  color:#2563eb;
  font-size:13px;
  line-height:1;
  font-weight:800;
  letter-spacing:.08em;
}
.howtoStep__body h3{
  margin:0 0 8px;
  font-size:22px;
  line-height:1.35;
  color:var(--title);
  font-weight:700;
}
.howtoStep__body p{
  margin:0;
  font-size:15px;
  line-height:1.7;
  color:#6f7b90;
}
.howtoPanels{
  display:grid;
  gap:16px;
  max-width:820px;
}
.howtoPanel{
  padding:22px 24px 20px;
  border-radius:16px;
  background:rgba(255,255,255,.96);
  border:1px solid #edf1f8;
  box-shadow:0 14px 32px rgba(148,163,184,.08);
}
.howtoPanel__eyebrow{
  display:block;
  margin-bottom:10px;
  color:#3159d6;
  font-size:12px;
  line-height:1;
  font-weight:800;
  letter-spacing:.09em;
}
.howtoPanel__select{
  min-height:68px;
  padding:0 20px;
  border:1px solid #e9eef8;
  border-radius:12px;
  background:#fff;
  display:flex;
  align-items:center;
  justify-content:space-between;
  color:#1f2a44;
  font-size:22px;
  line-height:1;
  font-weight:500;
}
.howtoPanel__caret{
  color:#3159d6;
  font-size:18px;
}
.howtoPanel__caption{
  margin:12px 0 0;
  color:#8a94a8;
  font-size:17px;
  line-height:1.65;
}
.howtoPanel__transform,
.howtoPanel__download{
  min-height:84px;
  padding:6px 8px 0;
  display:flex;
  align-items:center;
  justify-content:center;
  gap:18px;
}
.howtoPanel__fontSample{
  color:#19233c;
  white-space:nowrap;
}
.howtoPanel__fontSample--latin{
  font-family:"Nanum Myeongjo", serif;
  font-size:36px;
  line-height:1;
}
.howtoPanel__fontSample--korean{
  font-size:40px;
  line-height:1;
  font-weight:600;
}
.howtoPanel__connector{
  flex:0 0 44px;
  width:44px;
  height:1px;
  background:
    linear-gradient(90deg, rgba(59,130,246,.18), rgba(59,130,246,.18)) center/100% 1px no-repeat,
    radial-gradient(circle, rgba(59,130,246,.95) 0 1.5px, transparent 1.6px) left center/18px 8px repeat-x;
  animation:howtoConnectorWave 1.6s linear infinite;
}
.howtoPanel__loader{
  flex:0 0 auto;
  width:46px;
  height:46px;
  border-radius:12px;
  display:grid;
  place-items:center;
}
.howtoPanel__loaderSpin{
  width:46px;
  height:46px;
  border-radius:12px;
  background:linear-gradient(135deg, #2457ff, #3468ff);
  box-shadow:0 8px 16px rgba(49,89,214,.18);
  display:grid;
  place-items:center;
  animation:howtoLoaderSpin 2.2s linear infinite;
}
.howtoPanel__loaderCore{
  color:#fff;
  font-size:18px;
  line-height:1;
  font-weight:700;
}
.howtoPanel__downloadButton{
  min-width:162px;
  height:42px;
  padding:0 18px;
  border:1px solid #5e7bff;
  border-radius:999px;
  background:#fff;
  color:#3159d6;
  font-size:13px;
  line-height:1;
  font-weight:700;
  cursor:default;
  display:inline-flex;
  align-items:center;
  justify-content:center;
  gap:6px;
}

.howtoPanel__download{
  justify-content:center;
  gap:36px;
  padding-left:0;
}

.howtoPanel__download .howtoPanel__fontSample--korean{
  min-width:88px;
  text-align:center;
}
.howtoPanel__downloadIcon{
  font-size:14px;
  line-height:1;
}

@keyframes howtoLoaderSpin{
  from{transform:rotate(0deg)}
  to{transform:rotate(360deg)}
}

@keyframes howtoConnectorWave{
  0%{
    background-position:center center, 0 center;
  }
  100%{
    background-position:center center, 18px center;
  }
}

@media (max-width: 960px){
  .howto{
    padding:88px 0;
  }

  .howto__title{
    margin-bottom:40px;
    font-size:36px;
  }

  .howtoFlow{
    grid-template-columns:1fr;
    gap:28px;
  }

  .howtoSteps{
    padding:0;
  }

  .howtoSteps__line{
    left:31px;
    top:48px;
    bottom:48px;
  }

  .howtoStep__body h3{
    font-size:20px;
  }

  .howtoStep__body p{
    font-size:14px;
  }

  .howtoPanel__select{
    font-size:20px;
  }

  .howtoPanel__caption{
    font-size:16px;
  }

  .howtoPanel__fontSample--latin{
    font-size:30px;
  }

  .howtoPanel__fontSample--korean{
    font-size:32px;
  }
}

@media (max-width: 720px){
  .howto{
    padding:72px 0;
  }

  .howto__title{
    font-size:32px;
  }

  .howtoStep{
    grid-template-columns:42px 1fr;
    gap:12px;
  }

  .howtoStep__icon{
    width:42px;
    height:42px;
    border-radius:12px;
  }

  .howtoSteps__line{
    left:21px;
  }

  .howtoPanel{
    padding:14px 12px 12px;
  }

  .howtoPanel__select{
    min-height:48px;
    padding:0 14px;
    font-size:17px;
  }

  .howtoPanel__transform,
  .howtoPanel__download{
    min-height:56px;
    padding:0 4px;
    gap:10px;
  }

  .howtoPanel__download{
    gap:22px;
    padding-left:12px;
  }

  .howtoPanel__fontSample--latin{
    font-size:22px;
  }

  .howtoPanel__fontSample--korean{
    font-size:24px;
  }

  .howtoPanel__loader,
  .howtoPanel__loaderSpin{
    width:30px;
    height:30px;
    border-radius:9px;
  }

  .howtoPanel__downloadButton{
    min-width:118px;
    height:30px;
    font-size:11px;
  }

  .howtoStep__body p,
  .howtoPanel__caption{
    font-size:15px;
  }
}

@media (prefers-reduced-motion: reduce){
  .howtoPanel__loaderSpin{
    animation:none;
  }

  .howtoPanel__connector{
    animation:none;
  }
}

/* Community (而ㅻ??덊떚/由щ럭 ?뱀뀡) */
.community{padding-top:48px; padding-bottom:48px}
.community__title{
  margin:0 0 4px;
  font-size:24px;
  line-height:32px;
  font-weight:700;
}
.community__desc{
  margin:0;
  font-size:14px;
  line-height:20px;
  color:var(--muted);
}
.link-more{
  color:var(--primary);
  text-decoration:none;
  font-size:14px;
  font-weight:500;
  display:inline-flex;
  gap:6px;
  align-items:center;
}
.link-more:hover{opacity:.8}

.gallery{
  display:grid;
  grid-template-columns:repeat(3, minmax(0,1fr));
  gap:24px;
  align-items:start;
}
.gallery--reveal .tile{
  opacity:0;
  transform:translateY(22px);
  filter:blur(2px);
  pointer-events:none;
  transition:opacity .45s ease, transform .45s ease, filter .45s ease;
}
.gallery--reveal.gallery--count-3 .tile:nth-child(-n+3),
.gallery--reveal.gallery--count-6 .tile:nth-child(-n+6),
.gallery--reveal.gallery--count-9 .tile:nth-child(-n+9),
.gallery--reveal.gallery--count-12 .tile:nth-child(-n+12){
  opacity:1;
  transform:translateY(0);
  filter:none;
  pointer-events:auto;
}
.tile{
  border-radius:var(--r-16);
  padding:24px;
  display:flex;
  flex-direction:column;
  justify-content:space-between;
  box-shadow:var(--shadow-1);
  border:1px solid var(--card-border);
  background:var(--surface-base);
  min-height:160px;
}
.tile--h160{min-height:160px}
.tile--h176{min-height:176px}
.tile--h192{min-height:192px}
.tile--h208{min-height:208px}
.tile--h224{min-height:224px}
.tile--h256{min-height:256px}
.tile--h320{min-height:320px}

.tile--blue{
  background:var(--primary);
  border-color:transparent;
  box-shadow:var(--shadow-3);
}
.tile--dark{
  background:var(--text-strong);
  border-color:transparent;
  box-shadow:var(--shadow-2);
}
.tile--black{
  background:var(--ink);
  border-color:transparent;
  box-shadow:var(--shadow-2);
}
.tile--night{
  background:#1e232f;
  border-color:transparent;
  box-shadow:var(--shadow-3);
}
.tile--soft{background:var(--bg-softer)}
.tile--accent{background:var(--pill-bg)}
.tile--white{background:var(--surface-base)}

.tile__center{
  flex:1 1 auto;
  display:grid;
  place-items:center;
  text-align:center;
}
.tile__big{
  font-size:24px;
  line-height:32px;
  font-weight:700;
}
.tile--blue .tile__big,
.tile--dark .tile__big,
.tile--black .tile__big{color:var(--surface-base)}

.tile__sub{
  margin-top:8px;
  font-size:12px;
  line-height:16px;
  color:var(--muted-2);
}
.tile__mid{
  font-size:20px;
  line-height:28px;
  font-weight:500;
  color:var(--text-body-strong);
}
.tile__code{
  font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", monospace;
  font-size:18px;
  line-height:28px;
  color:var(--text-body-2);
}
.tile__display{
  font-size:36px;
  line-height:40px;
  font-family: "Playfair Display", Georgia, serif;
  color:var(--text-body-strong);
}
.tile__popular{
  font-size:20px;
  line-height:28px;
  font-weight:700;
  color:var(--primary);
}
.tile--kanji .tile__center{position:relative}
.tile__ghost{
  position:absolute;
  font-size:60px;
  line-height:60px;
  font-weight:700;
  color:var(--bg-soft);
  translate:0 -8px;
}
.tile__kanji{
  position:relative;
  z-index:1;
  font-size:30px;
  line-height:36px;
  font-weight:700;
  color:var(--text-body-strong);
}

.tile__footer{
  padding-top:13px;
  margin-top:24px;
  border-top:1px solid rgba(31,41,55,.15);
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:12px;
  font-size:12px;
  line-height:16px;
  color:var(--muted);
}
.tile__footer--light{
  border-top-color:rgba(156,38,38,.2);
  color:rgba(255,255,255,.8);
}
.tile__footer--muted{
  border-top-color:var(--card-border);
  color:var(--muted-2);
}
.tile__footer--accent{
  border-top-color:rgba(6,25,223,.15);
  color:rgba(6,25,223,.6);
}
.tile--dark .tile__footer,
.tile--black .tile__footer,
.tile--night .tile__footer{border-top-color:var(--text-body-strong)}
.tile--dark .tile__footer{color:var(--muted)}
.tile--night .tile__footer{color:var(--muted)}

.stat{display:inline-flex; gap:6px; align-items:center; white-space:nowrap}
.stat--light{color:rgba(255,255,255,.8)}
.stat--red{color:#ef4444}
.stat--pink{color:var(--accent-pink)}
.stat--salmon{color:var(--danger-soft)}

.dot{
  width:10px;height:10px;
  border-radius:999px;
  background:var(--border-strong);
  display:inline-block;
}
.dot--accent{background:rgba(6,25,223,.35)}

/* Footer */
.footer{
  background:var(--bg-soft);
  border-top:1px solid var(--border);
  padding:48px 0;
  margin-top:0;
}
.footer__top{
  display:grid;
  grid-template-columns: 1.4fr 1fr 1fr 1fr;
  gap:32px;
  align-items:start;
  margin-bottom:33px;
}
.footer__brandCol{display:flex; flex-direction:column; gap:16px}
.footer__brand{display:flex; align-items:center; gap:8px}
.footer__logoImg{
  height:32px;
  display:block;
}
.footer__tagline{
  margin:0;
  font-size:14px;
  line-height:20px;
  color:var(--muted);
}
.social{display:flex; gap:16px; height:24px}
.social__btn{
  width:24px;height:24px;
  border-radius:6px;
  background:rgba(17,24,39,.12);
  display:inline-block;
  text-decoration:none;
}
.chip--top10::after{
  content:"\2192";
  margin-left:8px;
}

.footer__head{
  font-size:14px;
  line-height:20px;
  font-weight:700;
  letter-spacing:.05em;
  text-transform:uppercase;
  color:var(--text);
  margin-bottom:16px;
}
.footer__col{display:flex; flex-direction:column; gap:12px}
.footer__link{
  font-size:16px;
  line-height:24px;
  color:var(--muted);
  text-decoration:none;
}
.footer__link:hover{opacity:.8}

.footer__bottom{
  border-top:1px solid var(--border);
  padding-top:33px;
  display:flex;
  align-items:flex-start;
  justify-content:space-between;
  gap:24px;
}
.footer__addr{
  font-size:14px;
  line-height:20px;
  color:var(--muted-2);
}
.footer__copy{
  font-size:14px;
  line-height:20px;
  color:var(--muted-2);
  text-align:right;
  white-space:nowrap;
}

/* Responsive */
@media (max-width: 1024px){
  .heroSlideFrame{
    min-height:unset;
  }
  .hero__grid{
    display:flex;
    /* ?쒕툝由?援ш컙 醫뚯슦 釉붾줉 媛꾧꺽 */
    gap:20px;
    padding-right:10px;
  }
  .hero__grid--existing{
    display:block;
    min-height:400px;
    padding-right:486px;
  }
  .hero__cards{
    flex:0 0 456px;
    width:456px;
    max-width:456px;
    margin-left:auto;
  }
  .hero__grid--existing .hero__cards{
    /* ?쒕툝由우뿉?쒕룄 ?곗륫 移대뱶 ?대룞媛??좎? */
    right:-90px;
  }
  .hero__copy h1{
    font-size:48px;
    line-height:54px;
  }
  .hero__titleTwoLine{
    font-size:40px;
    line-height:48px;
  }
  .hero__titleTwoLine > span:first-child{
    /* ?쒕툝由? 1踰?諛곕꼫 泥?以??쒕ぉ ?ш린 */
    font-size:50px;
    line-height:54px;
  }
  .hero__titleTwoLine > span:last-child{
    /* ?쒕툝由? 1踰?諛곕꼫 ?섏㎏ 以??쒕ぉ ?ш린 */
    font-size:35px;
    line-height:40px;
  }
  .hero__desc{
    /* ?쒕툝由? 1踰?諛곕꼫 蹂댁“ ?ㅻ챸 ?ш린/以꾧컙寃?*/
    font-size:20px;
    line-height:1.4;
  }
  .heroPromo{
    grid-template-columns:1fr;
    gap:28px;
  }
  .heroPromo--scan{
    grid-template-columns:1fr;
  }
  .heroPromo__title{
    font-size:44px;
    line-height:50px;
  }
  .heroPromo__title--scan{
    font-size:52px;
    line-height:1.02;
  }
  .heroPromo__desc{
    font-size:20px;
  }
  .heroPromo__right{
    justify-content:center;
    padding-right:0;
  }
  .heroPromo__right--scan{
    min-height:360px;
  }
  .scanScene{
    width:min(100%, 540px);
    min-height:390px;
  }
  .heroPromo__card{
    width:360px;
  }
  .cards--4{grid-template-columns:repeat(2,minmax(0,1fr))}
  .featureFontsGrid{grid-template-columns:repeat(2,minmax(0,1fr))}
  .featureFontsGrid--threeUp{grid-template-columns:repeat(2,minmax(0,1fr))}
  .featureFontsToolbar{gap:12px}
  .gallery{grid-template-columns:repeat(2,minmax(0,1fr))}
  .footer__top{grid-template-columns:1fr 1fr}
}
@media (max-width: 900px){
  .hero__grid{
    display:grid;
    grid-template-columns:1fr;
    gap:24px;
    padding-right:0;
  }
  .hero__grid--existing{
    min-height:unset;
    padding-right:0;
  }
  .hero__cards{
    flex:0 1 auto;
    justify-self:stretch;
    width:100%;
    max-width:none;
    min-height:unset;
    margin-left:0;
  }
  .hero__grid--existing .hero__cards{
    position:static;
    transform:none;
  }
}
@media (max-width: 640px){
  .header__top{flex-wrap:wrap; align-items:center}
  .brand{
    flex-basis:172px;
  }
  .brand__logoImg,
  .brand__logoImg--handwriting{
    height:68px;
  }
  .search{order:3; flex-basis:100%; max-width:none}
  .hero{
    padding:24px 24px 20px;
  }
  .hero__copy h1{
    font-size:36px;
    line-height:44px;
  }
  .hero__titleTwoLine{
    font-size:30px;
    line-height:38px;
  }
  .hero__titleTwoLine > span:first-child{
    font-size:36px;
    line-height:40px;
  }
  .hero__titleTwoLine > span:last-child{
    font-size:25px;
    line-height:30px;
  }
  .hero__desc{
    font-size:16px;
    line-height:1.4;
  }
  .heroSlideFrame{
    min-height:unset;
  }
  .heroPromo__title{
    font-size:34px;
    line-height:40px;
  }
  .heroPromo__title--scan{
    font-size:42px;
    line-height:1.04;
  }
  .heroPromo__desc{
    font-size:16px;
  }
  .heroPromo__actions{
    flex-direction:column;
    width:100%;
  }
  .heroPromo__btn{
    width:100%;
    min-width:0;
  }
  .heroPromo__social{
    flex-direction:column;
    align-items:flex-start;
  }
  .heroPromo__right{
    min-height:340px;
  }
  .scanScene{
    min-height:320px;
    transform:translateY(10px);
  }
  .scanScene__glyph{
    font-size:132px;
  }
  .scanScene__card--processing{
    right:0;
  }
  .heroPromo__card{
    width:100%;
    max-width:320px;
    min-height:320px;
  }
  .heroPromo__cardTitle{
    white-space:normal;
    font-size:22px;
  }
  .hero__cards{
    height:370px;
  }
  .pv--glass{
    width:min(300px, calc(100% - 52px));
  }
  .heroPreviewScroller{
    padding:84px 18px 172px 82px;
  }
  .nav{gap:16px}
  .cards--4{grid-template-columns:1fr}
  .featureFontsGrid{grid-template-columns:1fr}
  .featureFontsGrid--threeUp{grid-template-columns:1fr}
  .featureFontsToolbar{width:100%; justify-content:space-between}
  .featureFontsPager__button{width:34px; height:34px; font-size:20px}
  .featureFontCard__preview{min-height:264px; padding:22px 20px 18px}
  .featureFontCard__glyph{font-size:72px; gap:8px}
  .featureFontCard__glyph--wordmark{font-size:48px}
  .featureFontCard__headline{font-size:15px}
  .featureFontCard__copy{font-size:13px}
  .featureFontCard__body{padding:18px 18px 16px}
  .featureFontCard__actions{justify-content:flex-end}
  .steps{grid-template-columns:1fr}
  .steps__line{display:none}
  .gallery{grid-template-columns:1fr}
  .footer__top{grid-template-columns:1fr}
  .footer__bottom{flex-direction:column}
  .footer__copy{text-align:left; white-space:normal}
}








